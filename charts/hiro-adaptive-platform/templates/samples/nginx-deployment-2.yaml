{{- if .Values.samples.enabled }}
# Target of orchestrationprofile-2.yaml (via its targetDeploymentName —
# kept in sync by hand, see that file).
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.samples.nginxApp2.deploymentName }}
  namespace: {{ .Values.samples.namespace }}
  labels:
    app: {{ .Values.samples.nginxApp2.deploymentName }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ .Values.samples.nginxApp2.deploymentName }}
  template:
    metadata:
      labels:
        app: {{ .Values.samples.nginxApp2.deploymentName }}
    spec:
      containers:
        - name: nginx-2
          image: {{ .Values.samples.nginxImage }}
          ports:
            - containerPort: 80
          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
            limits:
              cpu: "100m"
              memory: "128Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.samples.nginxApp2.serviceName }}
  namespace: {{ .Values.samples.namespace }}
  labels:
    app: {{ .Values.samples.nginxApp2.deploymentName }}
spec:
  selector:
    app: {{ .Values.samples.nginxApp2.deploymentName }}
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP
{{- end }}
