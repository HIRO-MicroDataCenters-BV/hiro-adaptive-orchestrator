---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: orchestrationprofiles.orchestration.hiro.io
spec:
  group: orchestration.hiro.io
  names:
    kind: OrchestrationProfile
    listKind: OrchestrationProfileList
    plural: orchestrationprofiles
    shortNames:
    - op
    singular: orchestrationprofile
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: OrchestrationProfile is the Schema for the orchestrationprofiles
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of OrchestrationProfile
            properties:
              applicationRef:
                description: applicationRef references the application for which this
                  orchestration profile is defined.
                properties:
                  apiVersion:
                    type: string
                  kind:
                    type: string
                  name:
                    type: string
                  namespace:
                    type: string
                required:
                - apiVersion
                - kind
                - name
                - namespace
                type: object
              placement:
                description: placement defines the placement strategy and awareness
                  for the application.
                properties:
                  awareness:
                    description: Placement Awareness
                    properties:
                      cpu:
                        type: boolean
                      energy:
                        type: boolean
                      gpu:
                        type: boolean
                      memory:
                        type: boolean
                    type: object
                  strategy:
                    type: string
                required:
                - strategy
                type: object
              rebalancing:
                description: rebalancing defines the rebalancing strategy for the
                  application.
                properties:
                  cooldownSeconds:
                    type: integer
                  enabled:
                    type: boolean
                  triggerConditions:
                    items:
                      type: string
                    type: array
                required:
                - enabled
                type: object
            required:
            - applicationRef
            - placement
            type: object
          status:
            description: status defines the observed state of OrchestrationProfile
            properties:
              lastUpdatedTime:
                format: date-time
                type: string
              placementStatus:
                description: placementStatus provides details about the current placement
                  of the application.
                properties:
                  observedPods:
                    type: integer
                  pendingPods:
                    type: integer
                  podStatuses:
                    items:
                      description: Pod Status
                      properties:
                        id:
                          type: string
                        name:
                          type: string
                        namespace:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                      type: object
                    type: array
                  readyPods:
                    type: integer
                  strategy:
                    type: string
                type: object
              rebalancingStatus:
                description: rebalancingStatus provides details about the current
                  rebalancing state of the application.
                properties:
                  lastTriggeredAt:
                    type: string
                  reason:
                    type: string
                type: object
              status:
                description: status indicates the status of the orchestration profile
                  (e.g., "Pending", "Active", "Error").
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
