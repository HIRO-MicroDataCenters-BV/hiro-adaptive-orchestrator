apiVersion: v1
kind: Service
metadata:
    labels:
        app.kubernetes.io/component: placement-server
        app.kubernetes.io/name: {{ include "hiro-adaptive-orchestrator.name" . }}
        helm.sh/chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
        app.kubernetes.io/instance: {{ .Release.Name }}
        app.kubernetes.io/managed-by: {{ .Release.Service }}
        control-plane: controller-manager
    name: {{ include "hiro-adaptive-orchestrator.resourceName" (dict "suffix" "controller-manager-placement-service" "context" $) }}
    namespace: {{ .Release.Namespace }}
spec:
    ports:
        - name: placement-api
          port: 8090
          protocol: TCP
          targetPort: 8090
    selector:
        app.kubernetes.io/name: {{ include "hiro-adaptive-orchestrator.name" . }}
        control-plane: controller-manager
    type: ClusterIP
