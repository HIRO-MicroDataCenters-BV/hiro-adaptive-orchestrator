{{- if .Values.rbacHelpers.enable }}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
    labels:
        app.kubernetes.io/managed-by: {{ .Release.Service }}
        app.kubernetes.io/name: {{ include "hiro-adaptive-orchestrator.name" . }}
        helm.sh/chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
        app.kubernetes.io/instance: {{ .Release.Name }}
    name: {{ include "hiro-adaptive-orchestrator.resourceName" (dict "suffix" "orchestrationprofile-editor-role" "context" $) }}
rules:
    - apiGroups:
        - orchestration.orchestration.hiro.io
      resources:
        - orchestrationprofiles
      verbs:
        - create
        - delete
        - get
        - list
        - patch
        - update
        - watch
    - apiGroups:
        - orchestration.orchestration.hiro.io
      resources:
        - orchestrationprofiles/status
      verbs:
        - get
{{- end }}
